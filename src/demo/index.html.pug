doctype html
html(lang='en')
    head
        meta(charset='utf-8')
        title #{pkg.title} #{pkg.version} Demo
        meta(name='description', content=`${pkg.title} ${pkg.version} Demo (${pkg.homepage})`)
        meta(name='viewport', content='width=device-width, initial-scale=1')
        link(href='normalize.css', rel='stylesheet')
        link(href='styles.css', rel='stylesheet')
        script(src='jquery.js')
        script(src='jquery.mousewheel.js')
        script(src=`../jquery-scrollpanel-${pkg.version}.js`)
        script(src='scripts.js')

    body
        h1
            a(href=pkg.homepage title=`${pkg.title} project page`) #{pkg.title}
            span  #{pkg.version} Demo

        div.scrollpanel.no1
        div.scrollpanel.no2
        div.scrollpanel.no3
        div.scrollpanel.no4
        div.scrollpanel.no5
